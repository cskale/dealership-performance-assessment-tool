

## Plan: Update Metrics Label

**File:** `src/pages/Index.tsx` — line 59

**Change:** Replace `"Assessment\nquestions"` with `"KPIs\nTracked"` in the METRICS array.

Single word change, nothing else modified.

